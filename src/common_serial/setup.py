#!/usr/bin/env python3
from setuptools import setup

package_name = 'common_serial'

setup(
    name=package_name,
    version='0.0.0',
    packages=[package_name],
    description='Shared Arduino serial helpers for ai_ws',
    license='MIT',
    install_requires=[],
)
